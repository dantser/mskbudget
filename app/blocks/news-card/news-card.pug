//- image - название файла с картинкой
//- category - категория новости
//- title - заголовок новости
//- date - дата новости
//- модификатор ._big для увеличенной карточки

mixin news-card(image, category, title, date, tags)
  +b.news&attributes(attributes)
    a(href="#").news__thumb
      img(src="assets/images/news/"+image+".jpg", alt="thumb")
    a(href="#").news__category= category
    a(href="#", title=title).news__title= title
    .news__bottom
      p.news__date= date
      .news__tags
        each tag in tags
          a(href="#")= '#' + tag
    .news__abs
      button.share__btn
      .share__panel
        +social('facebook-box', 'vk', 'tw', 'odnokl').__social

mixin event-card(image, title, date)
  +b.news._events&attributes(attributes)
    a(href="#").news__thumb
      img(src="assets/images/events/"+image+".jpg", alt="thumb")
    a(href="#").news__title= title
    .news__bottom
      p.news__date= date

mixin photo-card(image, surname, initials, position)
  +b.news._photo&attributes(attributes)
    a(href="#").news__thumb
      img(src="assets/images/smi-fin/"+image+".jpg", alt="thumb")
    +e.surname= surname
    +e.initials= initials
    +e.position= position
    .document__bottom
      A(href='#').document__dwnl Скачать (DOCX 957,55 Кб)
      
mixin logo-card(title, img)
  +b.news._logo&attributes(attributes)
    +e.title= title
    +e.logo-img
      img(src='assets/images/' + img + '.png', alt='thumb')
    .document__bottom
      A(href='#').document__dwnl Скачать (EPS 957,55 Кб)

mixin material-card(image, title, date)
  +b.news._material&attributes(attributes)
    a(href="#").news__thumb
      img(src="assets/images/smi-fin/"+image+".jpg", alt="thumb")
    .news__bottom
      a(href="#").news__title= title
      p.news__date= date

mixin news-card-dropdown(image, category, title, date, tags)
  +b.news._dropdown&attributes(attributes)
    a(href="#").news__thumb
      img(src="assets/images/news/"+image+".jpg", alt="thumb")
    a(href="#").news__category= category
    a(href="#").news__title= title
    .news__bottom
      p.news__date= date
      .news__tags
        each tag in tags
          a(href="#")= '#' + tag
    .news__abs
      button.share__btn
      .share__panel
        +social('facebook-box', 'vk', 'tw', 'odnokl').__social

mixin short-newscard(category, date, title)
  +b.short-newscard&attributes(attributes)
    +e.A(href="#").category= category
    +e.P.date= date
    +e.A(href="#").title= title

mixin news-cards(isSlider)
  +b.news-cards&attributes(attributes)
    if isSlider
      .tile.swiper-wrapper
        block
    else
      .tile
        block      