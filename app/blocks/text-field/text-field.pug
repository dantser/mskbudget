//- Модификаторы
//- _col - надпись над полем ввода

mixin text-field(ttype, value)
  - var tttype = ttype || 'text';

  +b.LABEL.text-field&attributes(attributes)
    +e.SPAN.label
      block
    +e.INPUT(type=tttype, value=value).text
