include ../../blocks/button/button
include ../favor-list/favor-list
include ../../blocks/image-bg/image-bg
include ../../blocks/text-field/text-field

mixin profile-edit()
  +b.profile.profile_edit&attributes(attributes)
    +e.info

      +e.head
        +e.H3.head-title Редактирование профиля

      +e.main
        +e.photo
          +e.photo-img
            img(src="assets/images/profile/person.jpg")
          +e.A(href='#').photo-edit Изменить фото

        +e.personal
          //+e.SPAN.email k.konstantinopolsky@gmail.com

          +e.FORM(action="" method="POST" novalidate).profile-form.js-form-validate
            +e.field._fio.disabled
              +text-field('text', 'Константин Константинопольский Константинович', 'fio')._col
                | ФИО
                +e.SPAN.change-field Изменить
            +e.field._email.disabled
              +text-field('email', 'k.konstantinopolsky@gmail.com', 'email')._col
                | E-mail
                +e.SPAN.change-field Изменить
            +e.SPAN.note Для смены пароля введите старый пароль и дважды новый
            +e.field._pass._old
              +text-field('password', '', 'oldpassword')._col
                | Старый пароль
            +e.field-wrapper
              +e.field._pass._new
                +text-field('password', '', 'password')._col
                  | Новый пароль
              +e.field._pass._repeat
                +text-field('password', '', 'repeatpassword')._col
                  | Повторите новый пароль

            +e.confirm
              +e.submit
                //- +button(true)._kind_yellow(data-tab-target="profile")
                +button(true)._kind_yellow
                  | Сохранить
              +e.cancel
                +button()._kind_transparent(data-tab-target="profile")
                  | Отменить

    //+e.favorite
    +favor-list().__favorite
