import $ from 'jquery';

export default () => {
  const TRIGGER = $('.quiz-hint-trigger');
  const HINT = $('.quiz-hint');
  const CLOSE = $('.d-si-content__quiz-hint-close');
  const TRIGGERHIDE_CLASS = 'hide';
  TRIGGER.on('click', () => {

  })

};
