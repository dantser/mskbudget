include ../page-header/page-header

mixin reset()
	+b.reset&attributes(attributes)
		.wrapper.page-content-wrapper
			
			+page-header('Восстановление пароля')

			+e.FORM(action='', method='POST').form

				+e.field._d1
					+e.LABEL(for='r-newpass').label Новый пароль
					+e.INPUT(id='r-newpass', type='password', name='newpass', value='').text

				+e.field._d2
					+e.LABEL(for='r-repeatpass').label Повторите новый пароль
					+e.INPUT(id='r-repeatpass', type='password', name='repeatpass', value='').text
                    
				+e.P.errortext Пароли не совпадают

				+e.INPUT(type='submit', value='Отправить').submit
